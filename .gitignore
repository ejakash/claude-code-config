# Per-machine Claude Code state — never commit
.claude/

# Stale leftover from the old changelog-status sync model (now replaced
# by the synced-on-<hostname> git tag). If this file appears, it's
# git-ignored and harmless.
.changelog-status
